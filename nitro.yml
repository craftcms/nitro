name: nitro-server # the name of the virtual machine
system:
  php: 7.4 # this will be the default version of PHP installed on the machine
  database: mysql # which database engine should be bootstrapped
hosts: # maps multiple virtual servers onto the virtual machine
  - domain: nitro.test # the domain name the web server will respond
    path: /Users/jasonmccallister/Code/craftcms # the local (full) path to mount on the host machine
    php: 7.4 # nitro allows multiple versions of PHP, each virtual server can specify a version of PHP
